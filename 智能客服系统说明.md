# 旅游智能客服系统 · 说明文档

面向在线旅游行业的智能客服系统，仿照「电商小二」项目的分层架构实现，构建在
`travel` 业务数据之上，覆盖产品咨询、订单查询、出行信息查询、退款申请、工单提交等
高频服务场景，并具备完整的多轮对话管理与状态持久化能力。

## 1. 架构设计

与「电商小二」一一对应：

| 电商小二模块 | 本系统模块 | 职责 |
| --- | --- | --- |
| DialogueService | `app/dialogue/service.py` | 会话级入口，编排状态仓库与对话引擎 |
| DialogueState 设计与定义 | `app/dialogue/state.py` | 对话状态（任务、槽位、上下文、暂存任务） |
| DialogueStateRepository | `app/dialogue/state_repository.py` | 状态持久化、会话恢复、消息与轨迹记录 |
| DialogueEngine 设计/实现 | `app/dialogue/engine.py` | 一次对话的完整链路编排 |
| Planning 模块 | `app/dialogue/planning.py` | 意图规划（意图 → 轨道） |
| TaskHandler | `app/dialogue/handlers/task.py` | 任务型对话（配置化槽位收集 + 执行 Action） |
| KnowledgeHandler | `app/dialogue/handlers/knowledge.py` | FAQ / 政策 / 预订规则检索 |
| ChitchatHandler | `app/dialogue/handlers/chitchat.py` | 闲聊兜底与业务引导 |
| ClarifyResponder | `app/dialogue/responder.py` | 意图澄清 |
| 自定义 Action | `app/dialogue/actions/` | 对接 travel 库的业务查询 / 写入 |

一次完整对话的处理链路：

```
接收消息 → 对象槽位提取 → 意图识别 → Planning 规划轨道
        → 处理器执行（任务 / 知识 / 闲聊 / 澄清）→ 生成回复 → 状态更新 → 持久化
```

### 目录结构

```
app/dialogue/
├── state.py            # 对话状态设计
├── state_repository.py # 状态持久化仓库
├── intent.py           # 意图定义 + 规则识别器（可插拔）
├── extractors.py       # 槽位提取 + 中文日期解析
├── knowledge.py        # FAQ / 政策知识库与检索
├── planning.py         # Planning 模块
├── responder.py        # ClarifyResponder
├── engine.py           # DialogueEngine
├── service.py          # DialogueService
├── router.py           # FastAPI 接口
├── tables.py           # 对话系统所需表（幂等建表）
├── schemas.py          # 请求 / 响应模型
├── handlers/           # Task / Knowledge / Chitchat 处理器
├── actions/            # 自定义 Action（产品/订单/退款/工单）
└── static/index.html   # 前端调试 / 演示页面
```

## 2. 启动说明

1. 配置数据库连接 `.env`（与「问数」项目共用 `travel` 库）。
2. 初始化数据库并生成样本数据：

```bash
uv sync
uv run init_db.py                        # 建库建表
uv run -m generate.main --profile smoke  # 生成小规模样本数据（或 --profile full）
```

3. 启动服务：

```bash
uv run -m app.main
# 或 make run
```

4. 访问：

- 对话调试 / 演示页面：http://127.0.0.1:8000/dialogue-demo
- Swagger 文档：http://127.0.0.1:8000/docs（标签 `dialogue`）

> 对话系统所需的 `dialogue_sessions` / `dialogue_messages` / `service_tickets` 三张表
> 会在服务启动（或首次调用）时以 `CREATE TABLE IF NOT EXISTS` 幂等创建，不影响
> `sql/travel.sql` 的既有建表流程。

## 3. 接口说明

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/dialogue/sessions` | 创建对话会话 |
| GET | `/api/dialogue/sessions/{id}` | 获取会话状态（当前任务、已收集槽位） |
| GET | `/api/dialogue/sessions/{id}/messages` | 获取历史消息 |
| POST | `/api/dialogue/sessions/{id}/messages` | 发送消息（非流式） |
| POST | `/api/dialogue/sessions/{id}/messages/stream` | 发送消息（流式 SSE） |

请求体（发送消息）：

```json
{ "content": "帮我查下订单 ORD0000000001", "messageType": "text" }
```

业务对象消息：`messageType` 传 `order` / `product`，并在 `businessObject` 中携带
对象（如 `{"orderNo": "ORD0000000001"}`），系统会自动提取对象信息匹配当前任务槽位。

## 4. 支持的场景

- 产品咨询：酒店 / 景区 / 机票 / 火车票 / 汽车票 / 接送服务
- 订单状态查询：收集订单号 → 返回状态、金额、预订产品
- 出行信息查询：收集订单号（或出行日期）→ 返回行程安排、入住/出行人
- 退款申请：收集订单号 → 退款原因 → 退款类型 → 创建退款申请
- 工单提交：收集工单类型 → 关联订单号 → 问题描述 → 创建工单
- 政策咨询：退改政策、预订规则、出行须知、使用指南
- 闲聊兜底 / 意图澄清 / 流程切换·恢复·取消

## 5. 演示示例

```
旅客：三亚有哪些五星级度假酒店？
系统：为您找到 三亚 的相关酒店：
      - XX度假酒店（五星·度假型），起价 ¥xxx，地址：……

旅客：帮我查下订单 ORD0000000001
系统：订单 ORD0000000001 的信息如下：
      - 订单类型：酒店订单
      - 订单状态：已支付
      - …

旅客：我的订单 ORD0000000001 具体哪天入住？
系统：订单 ORD0000000001 的出行安排如下：……

旅客：我要退款
系统：请提供需要退款的订单号
旅客：ORD0000000001
系统：请说明退款原因
旅客：行程变更，全额退款
系统：退款申请已提交：申请单号 RR……，金额 ¥……，状态待处理

旅客：酒店房间和预订时描述的不一样，我要投诉
系统：请问您需要提交哪类工单？（售后 / 投诉 / 退款）
旅客：投诉
系统：请提供关联的订单号
旅客：无
系统：请简单描述您遇到的问题
旅客：房间有异味
系统：工单已提交成功：工单编号 TK……
```

## 6. 意图识别与可扩展性

- **意图识别**：采用「关键词加权 + 优先级」的规则识别，`IntentRecognizer` 以类接口
  对外暴露，可无缝替换为 LLM 识别实现而不改动编排逻辑。
- **任务流程**：`app/dialogue/handlers/task.py` 中的 `TASK_FLOWS` 配置化定义，
  新增任务只需增加配置，无需修改核心代码。
- **自定义 Action**：通过 `ActionRegistry` 注册，新增业务查询只需增加一个函数。

## 7. 测试

```bash
uv run pytest tests/test_dialogue.py       # 核心逻辑单元测试（无需数据库）
uv run pytest tests/test_dialogue_api.py   # 接口集成测试（需 make smoke）
```
